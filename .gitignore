.env
.env.mail
backups/
node_modules/
*.log
.idea/
deploy/build/
entrega/
# Contenido inicial de producción (produccion/db/garantiza-golden.sql.gz):
# SÍ se versiona, porque GitLab CI construye la imagen db desde el repo.
# Solo debe confirmarse la salida de scripts/generar-dump-limpio.sh, que
# aborta si el volcado contiene registros de formulario.
